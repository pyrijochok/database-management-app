<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Services</title>
    <style>
        #servicesProps{
            margin:10px;
        }
    </style>
</head>
<body>
    <h2>Service_type collection management</h2>
    <a href="/">Back</a>
    <button onclick="window.location.href = 'http://localhost:3000/services/add' ">Add service</button>
    <button onclick="window.location.href = 'http://localhost:3000/services/update' ">Update service</button>
    <button onclick="window.location.href = 'http://localhost:3000/services/delete' ">Delete service</button>
    <h3>Service_type collection data</h3>
    <% if (typeof message!=='undefined') { %>
        <p><%= message %></p>
      <% } else { %>
          <p></p>
      <% } %>
    <ul id="servicesList">
        <% for( let i = 0; i < services.length; i++ ) { %>
            <ul id="servicesProps">
                <li>Name: <%= services[i].Name %></li>
                <li>ID: <%= services[i]._id %></li> 
            </ul>
         <% } %>
    </ul>
    
   
</body>
</html>