<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add outtype</title>
    <style>
        input{
            display: block;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <form action="/outtypes/add" method="post">
        <% if (typeof invalidData !== 'undefined') { %>
            <label for="Name">Name:</label>
            <input type="text" name="Name" required value="<%= invalidData.Name %>">
        <% } else { %>
            <label for="Name">Name:</label>
            <input type="text" name="Name" required>
        <% } %>
        <button type="submit">Add outtype</button>
    </form>
    <% if (typeof message!=='undefined') { %>
      <p><%= message %></p>
    <% } else { %>
        <p></p>
    <% } %>
    <a href="/outtypes">Back</a>
</body>
</html>