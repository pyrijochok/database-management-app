<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Tourist</title>
    <style>
         input{
            display: block;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <form action="/tourists/delete?_method=DELETE" method="POST" id="deleteTouristf">
        <label for="_id">ID of tourist you want to delete:</label>
        <% if (typeof invalidId !== 'undefined') { %>
            <input type="text" name="_id" id="idField" required value="<%= invalidId %>">
            <% } else { %>
                <input type="text" name="_id" id="idField" required>
             <% } %>
        <button type="submit" >Delete tourist</button>
    </form>

    <% if (typeof message !=='undefined') { %>
      <p><%= message %></p>
    <% } else { %>
        <p></p>
    <% } %>

    <a href="/tourists">Back</a>
     
</body>
</html>